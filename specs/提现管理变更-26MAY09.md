# WalletRecord新增字段

|序号|字段|类型|必填|描述|备注|
|--|--|--|--|--|--|
|1|outer_type|int|否|外部数据类型|1-提现|
|1|outer_id|bigint|否|外部id||
|2|is_valid|bit|是|是否有效|默认1|

# 逻辑变更

1. 原有的审核通过再生成流水记录改为提现申请保存成功后生成，流水记录的outer_type=1，outer_id=提现申请id
2. 审核拒绝或者撤回申请后，对应的流水记录的is_valid改为false，并填写备注。
